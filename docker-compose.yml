services:
  next-dev:
    build: .
    volumes:
      - ./my-app:/app
    ports:
      - "3000:3000"
    working_dir: /app
    tty: true
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
